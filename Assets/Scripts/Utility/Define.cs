public class Define
{
    public enum PortType
    {
        Flow,
        Data,
    }
    public enum PortDirection
    {
        In,
        Out,
    }
    public enum DataType
    {
        None,
        Int,
        Bool,
        String,
    }

    public enum Layer
    {
        Interaction = 6,
    }
}